<%= provide(:title, 'All articles') %>
<h1>All articles</h1>

<% if signed_in? %>
	<p><%= link_to "New Article", new_article_path,
				 class: "btn btn-success"  %></p>
	<br>
<% end %>

<ul class="articles">
	<%= render @articles %>
</ul>
